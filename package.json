{
  "name": "music-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Private, end-to-end encrypted cloud storage for your music. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node test/crypto.test.js && node test/local.test.js && node test/server.test.js"
  }
}
